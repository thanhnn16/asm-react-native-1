import React from 'react';
import { View, Text } from 'react-native';

const AppointmentScreen = () => {
    return (
        <View>
            <Text>Hello AppointmentScreen</Text>
        </View>
    );
};

export default AppointmentScreen;
